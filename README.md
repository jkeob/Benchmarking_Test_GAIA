# GAIA Benchmark Runner (Meeting Notes Agent)

This repo runs a GAIA benchmark loop against a local agent endpoint and prints a score summary.

## Quick start

1. Create and activate venv:
```bash
python -m venv .venv
source .venv/bin/activate

